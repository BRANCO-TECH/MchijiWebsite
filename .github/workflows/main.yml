
name: Push to public repo

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build project
        run: |
          # Add your build commands here (e.g., npm run build)
      - name: Push to public repo
        uses: cpina/github-action-push-to-another-repository@v1.2
        with:
          source-directory: 'build'
          destination-github-username: 'BRANCO-TECH'
          user-email: 'mwaleb075@gmail.com'
          destination-repository-username: 'BRANCO-TECH/Mchinji-Secondary-Web-Portal'
          target-branch: 'main'
          ssh-key: ${{ secrets.SSH_DEPLOY_KEY }}
          commit-message: 'Deploy buil to public repo'



